PORT = 3000
PGPORT = 5432
PGHOST = localhost
PGUSER = postafenogresy
PGPASSWORD = postafenogresy
PGDATABASE = dadabe

# Generate strong random values for both, e.g.: openssl rand -base64
JWT_SECRET = your-strong-jwt-secret
API_KEY = your-strong-api-key
