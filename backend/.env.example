PORT = 3000
PGPORT = 5432
PGHOST = localhost
PGUSER = postgres
PGPASSWORD = postgres
PGDATABASE = school

# Generate strong random values for both, e.g.: openssl rand -base64 64
JWT_SECRET = your-strong-jwt-secret
API_KEY = your-strong-api-key